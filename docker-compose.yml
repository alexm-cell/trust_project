version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: trustia
      POSTGRES_USER: trustia
      POSTGRES_PASSWORD: trustia
    ports:
      - "5432:5432"

  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
